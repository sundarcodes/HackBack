angular.module('hackathonRatingApp').
service('RestAPIService',restAPIService);

restAPIService = function($resource) {
  restService = this;

  this.doUserSignUp = function(username,password) {

  };

  this.doUserLogin = function(username,password) {

  };

}
